required:
  - name
type: object
properties:
  name:
    type: string
    description: The name of the field.
  property:
    type: array
    description: >-
      When the field refers to a value in an entity property, details of the
      entity property value.
    items:
      $ref: ./JqlQueryFieldEntityProperty.yaml
additionalProperties: false
description: >-
  A field used in a JQL query. See [Advanced searching - fields
  reference](https://confluence.atlassian.com/x/dAiiLQ) for more information
  about fields in JQL queries.
