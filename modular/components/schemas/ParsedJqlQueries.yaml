required:
  - queries
type: object
properties:
  queries:
    minLength: 1
    type: array
    description: A list of parsed JQL queries.
    items:
      $ref: ./ParsedJqlQuery.yaml
additionalProperties: false
description: A list of parsed JQL queries.
