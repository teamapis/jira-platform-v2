type: object
properties:
  total:
    type: integer
    format: int64
  startAt:
    type: integer
    format: int64
  maxResults:
    type: integer
    format: int32
  results:
    type: array
    items:
      $ref: ./Comment.yaml
additionalProperties: false
