type: object
properties:
  total:
    type: integer
    format: int64
  results:
    type: array
    items:
      $ref: ./Comment.yaml
  maxResults:
    type: integer
    format: int32
  startAt:
    type: integer
    format: int64
additionalProperties: false
