type: object
properties:
  queries:
    type: array
    description: The list of sanitized JQL queries.
    items:
      $ref: ./SanitizedJqlQuery.yaml
additionalProperties: false
description: The sanitized JQL queries for the given account IDs.
