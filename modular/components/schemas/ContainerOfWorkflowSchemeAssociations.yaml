required:
  - values
type: object
properties:
  values:
    type: array
    description: >-
      A list of workflow schemes together with projects they are associated
      with.
    items:
      $ref: ./WorkflowSchemeAssociations.yaml
additionalProperties: false
description: >-
  A container for a list of workflow schemes together with the projects they are
  associated with.
