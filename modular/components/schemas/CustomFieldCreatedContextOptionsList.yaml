type: object
properties:
  options:
    type: array
    description: The created custom field options.
    items:
      $ref: ./CustomFieldContextOption.yaml
additionalProperties: false
description: A list of custom field options for a context.
